## slurm-cli submit

Submit a job

### Synopsis

Submit a new job to the SLURM cluster.

```
slurm-cli submit [flags]
```

### Options

```
  -c, --command string     Command to run (required)
      --cpus int           Number of CPUs (default 1)
  -h, --help               help for submit
  -m, --memory int         Memory in MB (default 1024)
  -n, --name string        Job name
  -p, --partition string   Partition name
  -t, --time int           Time limit in minutes (default 60)
  -w, --workdir string     Working directory
```

### Options inherited from parent commands

```
      --api-version string   API version (e.g., v0.0.42)
      --debug                Enable debug logging
  -o, --output string        Output format: table, json, yaml (default "table")
      --password string      Basic auth password (env: SLURM_PASSWORD)
      --token string         JWT authentication token (env: SLURM_JWT)
      --url string           SLURM REST API URL (env: SLURM_REST_URL)
      --username string      Basic auth username (env: SLURM_USERNAME)
```

### SEE ALSO

* [slurm-cli](slurm-cli.md)	 - CLI tool for SLURM REST API

###### Auto generated by spf13/cobra on 23-Jan-2026
